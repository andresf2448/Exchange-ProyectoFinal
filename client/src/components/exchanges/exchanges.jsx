import {Container, Typography} from '@material-ui/core';

export const Exchanges = ()=>{

    return (
        <Container>
            <Typography variant='h3'>soy other exchanges</Typography>
        </Container>
    )
}