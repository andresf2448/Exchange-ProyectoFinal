import {Container, Typography} from '@material-ui/core';

export const Wallet= ()=>{
    return(
        <Container>
            <Typography variant='h3'>soy wallet</Typography>
        </Container>
    )
}