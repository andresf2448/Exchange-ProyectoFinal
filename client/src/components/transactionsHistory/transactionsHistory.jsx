import {Container, Typography} from '@material-ui/core';

export const TransactionsHistory = ()=>{

    return (
        <Container>
            <Typography variant='h3'>soy Transactions History</Typography>
        </Container>
    )
}